# Used when Railway Root Directory = backend/
[build]
buildCommand = "npm run build"

[deploy]
startCommand = "npm run db:migrate && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
